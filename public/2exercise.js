document.addEventListener("DOMContentLoaded", () => {
  const a = 7
  const b = 11
  const sum = a + b
  const result = sum + 200 - 500
  alert("2exercise: The sum of " + a + " and " + b + " then add 200 and subtract 500 is: " + result)
})
