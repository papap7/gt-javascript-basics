document.addEventListener("DOMContentLoaded", () => {
  const a = 7
  const b = 11
  const c = 15
  const sum = a + b + c
  const result = sum + 100
  alert("1exercise: The sum of " + a + ", " + b + ", and " + c + " then add 100 is: " + result)
})
